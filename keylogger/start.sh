#!/usr/bin/env bash
# Make sure you have `logkeys` installed
# sudo apt-get install logkeys

#sudo logkeys -m $(pwd)/keymaps/en_GB.map -o $(pwd)/log/keys.log -s
sudo logkeys -m $(pwd)/keymaps/kek.map -o $(pwd)/log/keys.log -s
